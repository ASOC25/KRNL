You can not edit the folders alt/mlibc (excepting /alt/mlibc/sysdeps/krnl), packages and bundled
The main script for building is ./test.sh with flags --clean, --build, --debug and --run
The kernel code is in kernel/src and the userspace programs are in alt/sources.